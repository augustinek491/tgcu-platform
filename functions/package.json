{
  "name": "tgcu-functions",
  "version": "0.1.0",
  "private": true,
  "description": "TGCU platform Cloud Functions — server-owned writes (money/member/price/role/audit/deals).",
  "main": "lib/index.js",
  "engines": { "node": "20" },
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "serve": "npm run build && firebase emulators:start --only functions"
  },
  "dependencies": {
    "firebase-admin": "^13.0.2",
    "firebase-functions": "^6.3.0"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
